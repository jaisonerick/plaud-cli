# Deliberately empty. Importing the package used to pull in the transcription
# pipeline and, through it, torch — so a module with no such need, like the
# speaker store or the guest list, could not be imported without a GPU image.
